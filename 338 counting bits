/* Iterative => O(n)*32

class Solution {
    public int[] countBits(int n) {
        int[] ans=new int[n+1];
        for(int i=0;i<n+1;i++){
            int cnt=0;
            int mask=1;
            for(int j=0;j<32;j++){
                if((i&mask)!=0) cnt++;
                mask<<=1;
            }
            ans[i]=cnt;
        } 
        return ans;
    }
}
*/

class Solution {
    public int[] countBits(int n) {
        int[] ans = new int[n + 1];
        for (int i = 1; i < n + 1; i++) {
            ans[i] = ans[i >> 1] + (i & 1);
        }
        return ans;
    }
}